{
  "name": "pitchtrivia-ai-generator",
  "version": "1.0.0",
  "description": "Question Generator for Pitch Trivia",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install",
    "vercel-build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "dev": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "keywords": [
    "trivia",
    "ai",
    "question-generator"
  ],
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

